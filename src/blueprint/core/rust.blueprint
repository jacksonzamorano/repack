{ meta id: rust }
{ meta name: Rust }

{ define int64: i64 }
{ define int32: i32 }
{ define float64: f64 }
{ define string: String }
{ define boolean: bool }
{ define datetime: chrono::DateTime }

[ each object ]
pub struct {name} {
[ each field ]
	pub {name}: {if optional: Optional< }{type}{if optional: >}{if sep:,}
[ end each ]
}
[ end each ]

[ each enum ]
pub enum {name} {
[ each case ]
	{ name }{ if sep: , }
[ end each ]
}
[ end each ]
