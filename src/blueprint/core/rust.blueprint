[meta id]rust[/meta]
[meta name]Rust[/meta]

[define int64]i64[/define]
[define int32]i32[/define]
[define float64]f64[/define]
[define string]String[/define]
[define boolean]bool[/define]
[define datetime]chrono::DateTime[/define]

[file]model.rs[/file]
[each object]
pub struct [name] {
[each field]
	pub [name]: [if optional]Optional<[/if][type][if optional]>[/if][if sep],[/if]
[/each]
}
[/each]

[each enum]
pub enum [name] {
[each case]
	[name][if sep],[/if]
[/each]
}
[/each]
