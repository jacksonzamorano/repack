${meta id:rust/}
${meta name:Rust Compiler/}

${type int64}i64${/type}
${type string}string${/type}

${each object}
pub struct ${name}
	${each fields}
	pub ${name}: ${if optional:Option</}${type}${if optional}>${/if}
	${/each}
${/each}
